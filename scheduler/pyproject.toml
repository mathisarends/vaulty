[project]
name = "scheduler"
version = "0.1.0"
description = "Async scheduling primitives for agent heartbeats, reminders, and cron jobs."
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
    "aiosqlite>=0.22.0,<0.23",
    "croniter>=6.0.0,<7",
    "tzdata>=2024.1; sys_platform == 'win32'",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/scheduler"]

[tool.ruff]
extend = "../pyproject.toml"
